# Packages provided by system pacman/AUR packages — excluded from uv resolution.
# These are satisfied by the system site-packages in the venv and must NOT be
# reinstalled from PyPI (the PyPI torch wheels pull CUDA/nvidia dependencies).
#
# System packages:
#   python-pytorch-opt-rocm  -> torch
#   python-torchvision-rocm  -> torchvision  (AUR, ROCm build)
#   python-torchaudio-rocm        -> torchaudio   (AUR)
#   python-numpy             -> numpy
#   python-pandas            -> pandas
#   python-soundfile         -> soundfile
torch
torchvision
torchaudio
torchcodec
triton
numpy
pandas
soundfile
