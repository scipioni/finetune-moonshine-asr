# Core dependencies for Moonshine fine-tuning
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0,<4.0.0  # datasets 4.x+ requires torchcodec (CUDA-only); soundfile backend removed
accelerate>=0.24.0

# Training & Evaluation
evaluate>=0.4.0
jiwer>=3.0.0
tensorboard>=2.14.0
schedulefree>=1.4.0  # Schedule-free AdamW optimizer

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
torchaudio>=2.0.0

# Data handling
pyyaml>=6.0
tqdm>=4.66.0
numpy>=1.24.0
pandas>=2.0.0

# Optional: ONNX export and deployment
onnx>=1.14.0
onnxruntime>=1.16.0
optimum>=1.14.0  # For ONNX export and merged decoder

# Optional: Live transcription
# Install with: pip install -r requirements-live.txt
sounddevice>=0.5.0  # For live transcription mode

# Optional: Demo
gradio>=4.0.0

# Development
pytest>=7.0
black>=23.0
isort>=5.12
