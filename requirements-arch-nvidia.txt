# Requirements for Arch Linux with system PyTorch (CUDA/NVIDIA)
#
# The following packages must be installed from pacman/AUR BEFORE activating the venv:
#   sudo pacman -S python-pytorch-opt-cuda python-torchvision python-torchaudio \
#                  python-numpy python-pandas python-soundfile
#
# Then create the venv with system site-packages:
#   uv venv --system-site-packages --python /usr/bin/python .venv
#   source .venv/bin/activate
#   uv pip install --excludes excludes-arch-nvidia.txt -r requirements-arch-nvidia.txt

# Training & Evaluation
transformers>=4.35.0
datasets>=2.14.0,<4.0.0  # datasets 4.x+ requires torchcodec; soundfile backend removed
accelerate>=0.24.0
evaluate>=0.4.0
jiwer>=3.0.0
tensorboard>=2.14.0
schedulefree>=1.4.0

# Audio processing (soundfile and numpy come from system; torchaudio from PyPI — AUR package lags PyTorch)
librosa>=0.10.0
torchaudio>=2.10.0

# Data handling
pyyaml>=6.0
tqdm>=4.66.0

# Optional: ONNX export and deployment
onnx>=1.14.0
onnxruntime-gpu>=1.16.0
optimum>=1.14.0

# Development
pytest>=7.0
black>=23.0
isort>=5.12
